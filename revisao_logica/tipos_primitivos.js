// Strings, numbers(inteiros, flutuantes) e boolean.- .-.

// Variáveis primitivas sendo declaradas.- .-.
let variaveiString = "Anderson"; // String
let variaveiNumero = 29; // int
let variavelAltura = 1.76; // float
let variavelHumano = true; // boolean

let variavelDados = `String literals ${variaveiString}`;

console.log(variavelDados);

// typeof mostra o tipo de uma variável.- .-.
console.log(typeof variavelDados);

// Variáveis undefined são variáveis criadas sem definição.- .-.
let variavelUndefined;

// Variáveis null são variáveis que não possuem valor algum.- .-.
let variavelNull = null;